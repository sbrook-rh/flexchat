# ============================================================================
# ChromaDB Wrapper - Embedding Provider Credentials
# ============================================================================
# This service supports multiple embedding providers for creating and querying
# vector embeddings. The specific provider and model used for each collection
# is determined by the main config.json file in the chat backend.
#
# IMPORTANT: Configure credentials below for any providers you use in config.json
# ============================================================================

# Default Embedding Provider (fallback if not specified in API calls)
# Options: openai, gemini, ollama
EMBEDDING_PROVIDER=ollama

# ----------------------------------------------------------------------------
# OpenAI Configuration
# ----------------------------------------------------------------------------
# Required if your config.json uses: "type": "openai"
# The embedding_model should match the value in config.json providers section
# OPENAI_API_KEY=sk-your_openai_api_key_here
# OPENAI_EMBEDDING_MODEL=text-embedding-3-small  # Should match config.json

# ----------------------------------------------------------------------------
# Gemini Configuration
# ----------------------------------------------------------------------------
# Required if your config.json uses: "type": "gemini"
# The embedding_model should match the value in config.json providers section
# GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_EMBEDDING_MODEL=models/text-embedding-004  # Should match config.json

# ----------------------------------------------------------------------------
# Ollama Configuration
# ----------------------------------------------------------------------------
# Required if your config.json uses: "type": "ollama"
# The base_url and embedding_model should match config.json providers section
OLLAMA_BASE_URL=http://localhost:11434  # Should match config.json base_url
OLLAMA_EMBEDDING_MODEL=nomic-embed-text  # Should match config.json embedding_model
