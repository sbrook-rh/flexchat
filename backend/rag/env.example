# ============================================================================
# ChromaDB Wrapper Service Configuration
# ============================================================================
# This service provides HTTP access to ChromaDB for document storage and retrieval.
# All embeddings must be pre-computed and included in API requests.
#
# No environment variables required - configure via command line arguments:
#   python server.py --port 5006 --chroma-path ./chroma_db
# ============================================================================
