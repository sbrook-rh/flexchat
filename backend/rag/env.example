# ============================================================================
# ChromaDB Wrapper Service Configuration
# ============================================================================
# This service provides HTTP access to ChromaDB for document storage and retrieval.
# Embedding models are loaded at startup from YAML config file.
#
# Configure via command line arguments:
#   python server.py --embeddings-config embeddings.yml --port 5006 --chroma-path ./chroma_db
#
# Optional: Cross-encoder for reranking
#   python server.py --embeddings-config embeddings.yml --cross-encoder BAAI/bge-reranker-base
# ============================================================================

# ============================================================================
# Embedding Models Configuration
# ============================================================================
# Path to YAML file containing embedding model definitions
# Required for document storage and querying
# EMBEDDINGS_CONFIG=embeddings.yml

# ============================================================================
# Cross-Encoder Configuration (Optional)
# ============================================================================
# Cross-encoder model for document reranking
# CROSS_ENCODER_MODEL=BAAI/bge-reranker-base
# CROSS_ENCODER_PATH=/path/to/local/model
