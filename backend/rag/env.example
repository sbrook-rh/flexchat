# ============================================================================
# ChromaDB Wrapper - Storage-Only Service
# ============================================================================
# This service is a pure ChromaDB storage proxy.
# Node backend generates ALL embeddings (documents and queries).
# 
# NO ENVIRONMENT VARIABLES REQUIRED FOR EMBEDDING GENERATION
# ============================================================================

# No configuration needed - just run the service!
# Example: python server.py --port 5006 --chroma-path ./chroma_db
