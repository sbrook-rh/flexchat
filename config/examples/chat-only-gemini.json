{
  "$schema": "../schema/config-schema.json",
  "comment": "Simple chat-only configuration using Google Gemini API (no RAG)",
  
  "providers": {
    "gemini": {
      "type": "gemini",
      "apiKey": "${GEMINI_API_KEY}",
      "models": {
        "chat": "gemini-1.5-flash"
      },
      "comment": "Requires GEMINI_API_KEY environment variable. Get key from: https://makersuite.google.com/app/apikey"
    }
  },
  
  "knowledge_bases": {
    "comment": "No knowledge bases configured - pure chat mode"
  },
  
  "detection_provider": {
    "comment": "Not needed for single default strategy",
    "provider": "gemini",
    "model": "gemini-1.5-flash"
  },
  
  "strategies": [
    {
      "name": "DEFAULT",
      "comment": "Single strategy means no detection needed - all queries use this",
      "detection": {
        "type": "default"
      },
      "response": {
        "provider": "gemini",
        "model": "gemini-1.5-flash",
        "system_prompt": "You are a helpful AI assistant for Red Hat. Provide clear, accurate, and friendly responses.",
        "max_tokens": 1000,
        "temperature": 0.7
      }
    }
  ]
}

